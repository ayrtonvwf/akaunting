# Development log

**2026-08-12 — Ch1 audit:** Added [`AUDIT.md`](../../AUDIT.md) with the runtime and framework end-of-life positions, the CI matrix gap, a Laravel ceiling and reason for each of the 57 direct Composer dependencies, a coverage map over `app/` on both the framework-surface and product-domain axes, and the Ch3 Half B feasibility call. The call is feasible with named costs, re-targeted from Laravel 12 to 13 because 12's bug-fix window closed on 13 August 2026; the resolver probes fail on lock state rather than on any real ceiling, the four genuine ceilings are `akaunting/laravel-menu`, `genealabs/laravel-model-caching`, `intervention/imagecache` and `lorisleiva/laravel-search-string`, and section 5 orders them into what Ch3 can use as its `UPGRADE.md` skeleton. Generated evidence is committed under `audit-out/` and regenerated by `tools/audit/ceilings.php` and `tools/audit/coverage-map.php`; PCOV was added to `Dockerfile.local` for the one instrumented run. Verified by the scripts' own reconciliation checks, two Composer resolver probes, a determinism rerun, five packages spot-checked by hand, and `herd php artisan test`. The audit's own limits, including the unmeasured `modules/` surface and the single-source coverage cross-check, are stated in section 6.

**2026-08-07 — Graphify harness:** Added authoritative agent guidance for the repository-local Graphify setup in `AGENTS.md`. The guidance documents the repository-root rebuild wrapper (`tools/graphify/Invoke-Graphify.ps1`), output verifier (`tools/graphify/Test-GraphifyOutput.ps1`), locked query command against `graphify-out/graph.json`, required Composer-installed module manifests, constrained source scope (`app/`, `modules/`, `config/`, `routes/`, `tests/`), and the rule that only `EXTRACTED` edges count as structural evidence while `INFERRED` and `AMBIGUOUS` edges require source verification.

**2026-08-06 — Local environment:** Configured [Laravel Herd](https://herd.laravel.com/) as the Windows PHP and Composer environment, set PHP 8.2 as the global and Akaunting-specific version, linked the checkout at [http://akaunting.test](http://akaunting.test), and verified Laravel and Composer platform requirements through Herd. Use `herd use <version>` to switch the global PHP version, `herd isolate <version>` to pin this project, and `herd php artisan ...` or `herd composer ...` to run project commands with its isolated PHP version.

**2026-08-06 — Product reference:** Added the local, source-cited `docs/product/` reference for the Northstar harness. It maps all 22 Help Centre areas to local documentation, establishes core sales, purchases, banking, reporting, administration, optional-app, and extension vocabulary, distinguishes core, optional, and source-verified statements, reconciles extension guidance with local evidence, and records the corrected Invoices and Estimates citation. The completed set was checked with the documentation acceptance suite and `php artisan test`.
